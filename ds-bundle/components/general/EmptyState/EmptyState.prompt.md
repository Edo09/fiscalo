EmptyState from fiscalo. Use via `window.Fiscalo.EmptyState` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface EmptyStateProps {
  icon?: string;
  title: React.ReactNode;
  children?: React.ReactNode;
  action?: React.ReactNode;
}
```
