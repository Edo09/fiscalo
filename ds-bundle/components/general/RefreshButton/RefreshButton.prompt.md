RefreshButton from fiscalo. Use via `window.Fiscalo.RefreshButton` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface RefreshButtonProps {
  onRefresh: () => unknown;
  children?: React.ReactNode;
}
```
