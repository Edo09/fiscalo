import * as React from 'react';

/**
 * Checkbox — from fiscalo@1.0.0.
 */
export interface CheckboxProps {
on: boolean; onChange?: (value: boolean) => void;
}

export declare const Checkbox: React.ComponentType<CheckboxProps>;
