Checkbox from fiscalo. Use via `window.Fiscalo.Checkbox` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface CheckboxProps {
on: boolean; onChange?: (value: boolean) => void;
}
```
