Avatar from fiscalo. Use via `window.Fiscalo.Avatar` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface AvatarProps {
  name?: string;
  color?: string;
  size?: number;
  className?: string;
}
```
