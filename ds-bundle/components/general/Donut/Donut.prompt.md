Donut from fiscalo. Use via `window.Fiscalo.Donut` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface DonutProps {
segments: { value: number; color: string }[]; size?: number; thickness?: number;
}
```
