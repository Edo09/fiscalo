Sparkline from fiscalo. Use via `window.Fiscalo.Sparkline` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface SparklineProps {
data: number[]; color?: string; width?: number; height?: number;
}
```
