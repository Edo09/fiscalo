Spinner from fiscalo. Use via `window.Fiscalo.Spinner` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface SpinnerProps {
// Spinner takes no props — a fixed-size inline loading indicator.
}
```
