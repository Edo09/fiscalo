Btn from fiscalo. Use via `window.Fiscalo.Btn` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface BtnProps {
  variant?: "primary" | "secondary" | "ghost";
  size?: "" | "sm" | "lg";
  icon?: string;
  iconRight?: string;
  className?: string;
  id?: string;
  style?: CSSProperties;
  children?: React.ReactNode;
}
```
