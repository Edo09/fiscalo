Seg from fiscalo. Use via `window.Fiscalo.Seg` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface SegProps {
  options: string[];
  value: string;
  onChange: (value: string) => void;
}
```
