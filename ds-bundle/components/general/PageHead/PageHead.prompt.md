PageHead from fiscalo. Use via `window.Fiscalo.PageHead` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface PageHeadProps {
  title: React.ReactNode;
  sub?: React.ReactNode;
  crumbs?: Crumb[];
  actions?: React.ReactNode;
}
```
