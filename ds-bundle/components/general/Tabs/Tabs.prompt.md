Tabs from fiscalo. Use via `window.Fiscalo.Tabs` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface TabsProps {
  tabs: TabItem[];
  active: string;
  onChange: (id: string) => void;
}
```
