ErrorState from fiscalo. Use via `window.Fiscalo.ErrorState` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface ErrorStateProps {
  title?: string;
  children?: React.ReactNode;
  onRetry?: () => void;
}
```
