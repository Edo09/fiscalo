MenuItem from fiscalo. Use via `window.Fiscalo.MenuItem` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface MenuItemProps {
  icon?: string;
  children?: React.ReactNode;
  danger?: boolean;
  onClick?: () => void;
}
```
