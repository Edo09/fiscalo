Icon from fiscalo. Use via `window.Fiscalo.Icon` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface IconProps {
  name: string;
  size?: number;
  className?: string;
  style?: CSSProperties;
}
```
