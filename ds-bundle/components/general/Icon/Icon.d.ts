import * as React from 'react';

/**
 * Icon — from fiscalo@1.0.0.
 */
export interface IconProps {
  name: string;
  size?: number;
  className?: string;
  style?: CSSProperties;
}

export declare const Icon: React.ComponentType<IconProps>;
