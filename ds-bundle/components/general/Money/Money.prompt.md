Money from fiscalo. Use via `window.Fiscalo.Money` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface MoneyProps {
  value: number;
  cur?: boolean;
  className?: string;
  sign?: boolean;
}
```
