Card from fiscalo. Use via `window.Fiscalo.Card` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface CardProps {
  title?: React.ReactNode;
  sub?: React.ReactNode;
  actions?: React.ReactNode;
  children?: React.ReactNode;
  className?: string;
  noPad?: boolean;
}
```
