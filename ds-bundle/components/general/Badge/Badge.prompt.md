Badge from fiscalo. Use via `window.Fiscalo.Badge` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface BadgeProps {
  tone?: "neutral" | "accent" | "info" | "success" | "warning" | "danger";
  dot?: boolean;
  children?: React.ReactNode;
  className?: string;
}
```
