Switch from fiscalo. Use via `window.Fiscalo.Switch` (bundle loaded from the root `_ds_bundle.js`).

## Props

```ts
interface SwitchProps {
  on: boolean;
  onChange?: (value: boolean) => void;
}
```
